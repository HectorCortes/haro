# Exploration: v2-flujo-sdd — Development flow (SDD)

## Current State

### Normative contract (sources of truth)

- **Deltas `deltas-acceptance.md:1-501` (92 criteria, 11 specs):**
  - **`deltas-acceptance.md:1-60` header contract:**
    - `:1-8` Purpose block: line 7 states "A criterion is **met** only when its described verification passes reproducibly in the gentle-ai flow (see §Usage with gentle-ai)." Line 5 locks stack as "TypeScript is kept ... no rewrite ... ACP with Agent Client Protocol" — entirely stale for Haro Go (Go pure/no cgo since `v2-spike-go` 2026-08-28). Line 19 defines `id-spec` as "it matches the SDD change name in gentle-ai" — example `v2-broker`.
    - `:11-29` Conventions: ID hierarchy, verification types `[E2E]/[INT]/[UNIT]/[REV]/[PROC]`, priority `P0/P1/P2`, then `:25` "Decided stack — TypeScript (evolution of the current codebase, no rewrite): Node ≥ 20, ESM, `strict`. Test runner: `node --test` (explicit suite in `npm test`, ~550 cases). Typecheck: `tsc --noEmit`." — stale: real runner is `go test ./... -race` per `AGENTS.md:33` and `openspec/config.yaml:testing.runner`.
    - `:26` ACP paragraph about `acp-generic` adapter / `zod` validation — still accurate as protocol intent but wording assumes TS `src/schema/*`.
    - Tracking summary `:476-495`: table with 11 rows, last row `v2-flujo-gentle-ai | 4 | 3 | pending`, footer "Last updated: 2026-09-07 — `v2-distribucion` **complete**".
  - **All exact `gentle-ai` mentions in `deltas-acceptance.md` (verified via `grep -n`):**
    - `L7` — "in the gentle-ai flow (see §Usage with gentle-ai)" (Purpose `met` definition)
    - `L19` — "it matches the SDD change name in gentle-ai" (Conventions `id-spec`)
    - `L45` — table row `v2-flujo-gentle-ai | D09 | Development flow with gentle-ai`
    - `L49` — section heading `## Usage with gentle-ai`
    - `L51` — "or the gentle-ai equivalent) with the proposal → spec → design → tasks → apply → verify → archive cycle" (Usage flow)
    - `L456` — section heading `# Spec: v2-flujo-gentle-ai — Development flow with gentle-ai`
    - `L463` — F-02 criterion: "`sdd-verify` (or the equivalent verification of the gentle-ai flow) runs the described verifications"
    - `L467` — F-03 criterion: "each delta's delivery goes through the gentle-ai flow gates (review receipts, delivery gates)"
    - `L492` — tracking table pending row `v2-flujo-gentle-ai`
    - Total 9 hits across 7 logical locations (L7, L19, L45, L49+51 counted as Usage block, L456, L463, L467, L492).
  - **`§Usage with gentle-ai` `:49-57` (stale content beyond the name):**
    - `:49` heading `Usage with gentle-ai`
    - `:51` flow description: initiatives pipeline deprecated, SDD change with `proposal → spec → design → tasks → apply → verify → archive`, change name is `id-spec`.
    - `:54` "`sdd-verify` (or the equivalent verification) runs the verifications described here with `node --test` (`npm test`); the `[E2E]` ones are public boundary, the `[UNIT]`/`[INT]` ones are package/module tests." — `node --test`/`npm test` is stale Go drift; real is `go test ./...` layers per `openspec/config.yaml`.
    - `:55` "A delta is complete only when all its P0 and P1 criteria pass and the change is archived."
    - `:56` "No new initiatives are created; existing ones (e.g. 008) are migrated or explicitly closed."
  - **`v2-flujo-gentle-ai` spec `:456-473` (4 criteria, 3 P0 + 1 P1, all `[PROC]`/`[UNIT]`):**
    - `F-01 [PROC] P0 :459` — "each spec in this file (v2-reconciliacion, v2-no-regresion, v2-broker…v2-distribucion) is developed as an SDD change with proposal → spec → design → tasks → apply → verify → archive; the criteria in this file are the source of each spec's acceptance criteria." Verification: "one change per spec with criterion → spec → tests traceability."
    - `F-02 [PROC] P0 :462` — "`sdd-verify` (or the equivalent verification of the gentle-ai flow) runs the described verifications: the `[E2E]` ones as public boundary, the `[UNIT]`/`[INT]` ones as module tests." Verification: "each change's verification report lists the covered criteria with their evidence."
    - `F-03 [PROC] P0 :466` — "each delta's delivery goes through the gentle-ai flow gates (review receipts, delivery gates), not through the initiatives pipeline; no new initiatives are created." Verification: "delivery history with receipts; absence of new `.docs/initiatives/`."
    - `U-01 [UNIT] P1 :470` — "each criterion in this file has at least one named test verifying it (explicit mapping, e.g. table in the change's spec)." Verification: "audit script that walks the criterion IDs and confirms their corresponding test exists and passes."

- **`AGENTS.md:1-35`:**
  - `L7` single `gentle-ai` hit: "including gentle-ai/openspec/SDD artifacts" (Language section, technical artifacts naming). No other gentle-ai mention. Remaining `AGENTS.md` is already Go-correct except stale `Stack` line 23 "`go.mod` does not exist yet" (contradicts reality — `go.mod` exists since `v2-spike-go` with `module github.com/HectorCortes/haro`, `go 1.25.0`).
  - Development flow `:30-35`: SDD change cycle + Gate `go build / go vet / go test -race / golangci-lint / govulncheck` + Conventional commits — this is the real neutral flow already; no `gentle-ai` wording appears here.

- **`docs/v2/haro-constitucion.md` and `docs/v2/haro-especificacion-tecnica.md`:**
  - `grep -n gentle-ai` on both returns 0 hits — already neutral, no change needed (confirmed).
  - Constitution is compatible with rename: no `gentle-ai` provider name, no flow name tied to tooling; purely Haro regulations.

- **`openspec/` active vs archive:**
  - `grep -r gentle-ai openspec/` → 31 hits, all inside `openspec/changes/archive/*` (historical: `schema: gentle-ai.verify-result/v1`, mentions of `v2-flujo-gentle-ai` as next spec, `gentle-ai sdd-verify-validate`). **Zero hits** in `openspec/specs/` or `openspec/config.yaml` active specs — active layer already neutral. Archive is explicitly excluded per user decision ("TODO lo de `openspec/` queda EXCLUIDO — archivos históricos se conservan tal cual") — verified, no rename needed there.
  - Non-scope hits outside deltas/AGENTS: `.atl/skill-registry.md:3` auto-generated "gentle-ai skill-registry" header — tooling artifact, out of scope per user (only `deltas-acceptance.md` + `AGENTS.md` are in scope). `README.md`, scripts, Go code, workflows contain 0 hits.

### Codebase today — real development flow gates (verified, not assumed)

- **Module & runner (real, not TypeScript):** `go.mod` exists (`github.com/HectorCortes/haro`, `go 1.25.0`, `modernc.org/sqlite v1.57.0`, `gopkg.in/yaml.v3`). Runner is `go test ./...` with `-race` in CI, `go vet ./...`, `golangci-lint run`, `govulncheck ./...`. `openspec/config.yaml` documents `testing.runner.command: "go test ./..."`, layers `unit/integration/e2e` all true, `verify.test_command: "go test ./... -race"`, `verify.build_command: "go build ./..."`. `deltas-acceptance.md:25/54/147-148` claiming `node --test`/`npm test`/`tsc --noEmit` (550 cases, 74 suites) is stale v1 Shardeo residue; Haro's real suite is 14 passing packages (`go test ./... -race` green on 2026-09-07 baseline).
- **CI gates (`/.github/workflows/ci.yml` + `release.yml`):**
  - `ci.yml` (push main + PR): jobs `build-and-test` (`go build ./...` + `go vet ./...` + `scripts/verify-distribution.sh` + `go test ./... -race`), `lint` (`golangci-lint-action@v9`), `vulncheck` (`govulncheck-action@v1`). Branch protection requires `strict: true` + contexts `build-and-test`, `lint`, `vulncheck`, plus `required_pull_request_reviews: {dismiss_stale_reviews: true, require_code_owner_reviews: true, required_approving_review_count: 1}`, `required_linear_history: true`, `required_conversation_resolution: true`. This is the literal "delivery gates" for F-03 in Go terms.
  - `release.yml` (tags `v*` only): `verify` job (distribution gate + race tests + clean `GOPROXY=direct go install github.com/HectorCortes/haro@${GITHUB_REF_NAME}` + static build `CGO_ENABLED=0 go build -trimpath -ldflags="-s -w"` + checksum), then `release` job with sole `contents: write` grant. Proves D08's distribution heritage, not flow.
- **Delivery history — PR vs direct push (affects F-03 interpretation):**
  - Series precedent mixes both: `gh pr list` shows `feat/v2-reporte`, `feat/v2-composicion`, `feat/v2-adapter` were PRs (MERGED). But recent `git log --oneline` shows archive-phase commits (`docs(sdd): archive v2-distribucion`, `docs(sdd): archive v2-store`, `docs(sdd): archive v2-reporte`, `docs(sdd): archive v2-composicion`) are **direct pushes to `main`**, not PRs — allowed because they are `docs(sdd)` archive moves that happen after `sdd-verify` passed. `git log --merges` shows only 3 merges. So F-03's "review receipts, delivery gates" is satisfied for code-bearing PRs (branch protection enforces review gate), while archive docs steps ride direct pushes — a documented hybrid delivery the proposal must acknowledge as compliant with F-03's spirit (gates enforced where code changes, no initiatives pipeline used).
- **Initiatives pipeline:** `.docs/initiatives/` does not exist (`ls` returns "No such file or directory"), `tools/scripts/initiative/` does not exist. F-03's sub-clause "no new `.docs/initiatives/`" is satisfied trivially; no migration needed.
- **Series progress:** 92 criteria total, 11 specs. Archivados: `v2-reconciliacion` (complete), `v2-composicion`/`v2-reporte`/`v2-store`/`v2-distribucion` (complete per tracking table), plus older archives `v2-spike-go`, `v2-adapter`, `v2-no-regresion`, `v2-path-claims` (physically archived but tracking rows still `pending` per series convention — "archive cycles left `deltas-acceptance.md` untouched until composition/reporte era"). Diferidos: `v2-broker`/`v2-ipc` deferred per CLI-direct architecture. `v2-flujo-sdd` (`v2-flujo-gentle-ai`) is the **last pending spec** in the tracking table whose deferral does not apply (per `2026-09-07-v2-distribucion/archive-report.md:138`).
- **Audit script for U-01:** `scripts/` contains only `verify-adapter-boundary.sh`, `verify-distribution.sh`, `verify-store-boundary.sh` — boundary/import gates, not a criterion→test auditor. No script walks `deltas-acceptance.md` IDs and confirms a named test exists/passes. `grep -r "U-01|traceability|audit" scripts/` returns empty. U-01's verification clause ("audit script that walks the criterion IDs and confirms their corresponding test exists and passes") is therefore **unimplemented** — gap to close.
- **Prior traceability patterns (for U-01 sizing):**
  - Archived specs document traceability as `criterion → requirement → test/evidence` tables inside `spec.md` (traceability requirement) + `verify-report.md` compliance matrices. Example: `2026-09-07-v2-distribucion/spec.md` has `TRACE-D08` with 3/3 mapping + `verify-report.md` D08 `Criterion → Requirement → Real Test` table (7 scenarios, hashes). `2026-09-07-v2-store/spec.md` maps 6 criteria to real tests. Pattern is: spec adds a `TRACE` requirement mapping every Deltas criterion to a requirement/scenario, verify-report proves each scenario with `go test` evidence. U-01 for `v2-flujo-sdd` must apply the same pattern to its own 4 criteria **and** introduce the audit script that validates the whole 92-criteria ↔ test mapping — the latter is cross-cutting (not just D09's 4).
  - No archived change introduced a standalone `scripts/verify-traceability.sh` — this would be new. Size budgeted: `v2-adapter` shipped 3283 lines under `size:exception`, current `openspec/config.yaml:review_budget_lines: 20000` (hybrid store), `delivery_strategy: single-pr` — budget is not limiting.

## Gaps vs Criteria (expected vs current vs gap)

| Criterion | Expected (contract) | Current reality | Gap |
|-----------|---------------------|-----------------|-----|
| **F-01 Each spec as SDD change [PROC] P0** | Each delta spec (`v2-reconciliacion`…`v2-distribucion`) developed as SDD change with cycle `proposal → spec → design → tasks → apply → verify → archive`; criteria in `deltas-acceptance.md` are source of spec's acceptance criteria. Verification: one change per spec with criterion → spec → tests traceability. | All 11 deltas have had or will have one SDD change per spec: `v2-reconciliacion`, `v2-spike-go`, `v2-no-regresion`, `v2-adapter`, `v2-path-claims`, `v2-composicion`, `v2-reporte`, `v2-store`, `v2-distribucion` archived (9/11 with physical archive folders); `v2-broker`/`v2-ipc` intentionally deferred with CLI-direct architecture rationale documented in every archive report since `v2-spike-go`. Tracking table marks 5 as `complete`, 5 as `pending` (archives without row update) and `v2-flujo-gentle-ai` as final `pending`. Each archived change has `proposal → spec → design → tasks → verify → archive` artifacts in `openspec/changes/archive/`. No U-01 auditor yet to systematically prove "one change per spec" completed set — but the per-change evidence exists. | **Process-documentary gap only.** D09's own change must exist to close F-01 for the last spec; remaining work is to create `v2-flujo-sdd`'s own SDD artifacts (proposal/spec/design/tasks/verify/archive) and, for U-01's cross-cutting audit, prove the whole series' traceability. No code gap. Risk is that rename of the tracking id from `v2-flujo-gentle-ai` → `v2-flujo-sdd` must preserve history linkage (archive mentions of the old name remain). |
| **F-02 Verification via sdd-verify [PROC] P0** | `sdd-verify` (or equivalent) runs the described verifications: `[E2E]` as public boundary, `[UNIT]`/`[INT]` as module tests. Verification: each change's verification report lists covered criteria with evidence. | `sdd-verify` equivalent exists: `go test ./... -race` + `go build` + `go vet` + `golangci-lint` + `govulncheck`, orchestrated via `ci.yml` + `openspec/config.yaml:verify.*_command`. Every archived change has a `verify-report.md` with envelope `gentle-ai.verify-result/v1` (now to be read as SDD envelope; the schema prefix `gentle-ai.` is inside archive files — excluded from rename). Reports list `requirements: N/M`, `scenarios: N/M`, `test_command` hashes, `verdict: pass/pass_with_warnings`. The textual contract still says "`sdd-verify` (or the equivalent verification of the gentle-ai flow) runs … with `node --test` (`npm test`)" — stale runner. | **Wording gap only, plus neutral rename.** Real verification is Go tests; contract text must be updated to `sdd-verify (o la verificación equivalente del flujo SDD) ejecuta … con `go test ./...` (`go test ./... -race` en CI); los `[E2E]` como frontera pública, los `[UNIT]`/`[INT]` como tests de módulo.` Requires editing `deltas-acceptance.md:54` and F-02 criterion text `:463` while preserving envelope semantics. No behavior change. |
| **F-03 Delivery through the flow gates [PROC] P0** | Each delta's delivery goes through the flow gates (review receipts, delivery gates), not through initiatives pipeline; no new initiatives created. Verification: delivery history with receipts; absence of new `.docs/initiatives/`. | No `.docs/initiatives/` has been created in the series (verified absent). Delivery history is a hybrid: feature-bearing changes go via PRs with required reviews (branch protection `require_code_owner_reviews: true`, `required_approving_review_count: 1`, `dismiss_stale_reviews`, `required_conversation_resolution`), and CI gates `build-and-test/lint/vulncheck` are `strict: true`. Archive/documentation-only steps push directly to `main` (observed: `docs(sdd): archive …` commits on `main`, no PR). This matches the SDD deliver-single-prescription: gates enforced where code changes; no initiative pipeline used. Criterion text still says "gentle-ai flow gates" (`:467`) and Usage header says "with gentle-ai" (`:49`). | **Wording + interpretive clarification gap.** Must rename to "gates del flujo de desarrollo (receipts de review, gates de entrega)" and document that the hybrid PR-for-code / direct-push-for-archive pattern satisfies the gate intent. The `.docs/initiatives/` absence check must remain (and be added to the audit script). |
| **U-01 Criterion→test traceability [UNIT] P1** | Each criterion has at least one named test verifying it (explicit mapping, e.g. table in the change's spec). Verification: audit script that walks criterion IDs and confirms their corresponding test exists and passes. | Traceability exists per archived change as `spec.md` criterion→requirement→scenario tables + `verify-report.md` compliance matrices, but there is **no global audit script** that walks the 92 IDs and asserts each has a named test. `scripts/` has no such auditor. The per-change tables for D00/D05/D06/D07/D08 are complete; D01-D04 (broker/ipc/adapter/path-claims/no-regresion) have gaps or deferred rows still marked pending. For D09 itself, no `v2-flujo-sdd` spec table exists yet. | **Largest real gap.** Two sub-gaps: (a) global audit script (Go test or `scripts/verify-traceability.sh` + Go harness that runs it) that parses `deltas-acceptance.md` IDs (`grep -E "^### (F|U)-[0-9]+|^# Spec: v2-"` pattern), builds expected set, maps to test names listed in `openspec/specs/*/spec.md` and `verify-report.md` and asserts each ID has ≥1 test that exists and passes; (b) D09's own local table `v2-flujo-sdd/F-01…U-01 → requirement → test` inside its spec. Approach choice determines whether (a) is a real executable gate or a documented table-only proof. |

## Affected Areas

- **`deltas-acceptance.md`** — the only contract file to edit (per user scope). Exact edits required:
  - `L7` Purpose `met` clause: replace "in the gentle-ai flow (see §Usage with gentle-ai)" → "in the SDD flow (see §Usage)"
  - `L19` Conventions `id-spec` parenthetical: "in gentle-ai" → "in SDD" (or neutral "in the development flow")
  - `L45` tracking table id-spec: `v2-flujo-gentle-ai` → `v2-flujo-sdd`
  - `L49` section heading: `Usage with gentle-ai` → `Usage` (or `SDD development flow`)
  - `L51` flow sentence: remove "(or the gentle-ai equivalent)" and keep "implemented as an SDD change with the proposal → spec → design → tasks → apply → verify → archive cycle"
  - `L54` verify runner: `node --test` (`npm test`) → `go test ./...` (`go test ./... -race` in CI, locally at least `go test ./...`), and `[E2E]/[UNIT]/[INT]` layer note preserved
  - `L25` stack note: `TypeScript … Node ≥ 20 … node --test (~550 cases) … tsc --noEmit` → neutral Go stack (`Pure Go, no cgo, modernc.org/sqlite, go test ./... with -race, go vet, golangci-lint, govulncheck`)
  - `L5` header stack lock note "TypeScript is kept — no rewrite" → remove or replace with "Stack: Pure Go, no cgo — TypeScript evolution note is historical" (or delete; proposal should decide neutral wording)
  - `L456` spec heading: `# Spec: v2-flujo-gentle-ai — Development flow with gentle-ai` → `# Spec: v2-flujo-sdd — Development flow`
  - `L463` F-02: "`sdd-verify` (or the equivalent verification of the gentle-ai flow)" → "`sdd-verify` (or the equivalent verification of the SDD flow)" (ES: "o la verificación equivalente del flujo SDD")
  - `L467` F-03: "through the gentle-ai flow gates" → "through the development flow gates (review receipts, delivery gates)" (ES: "gates del flujo de desarrollo")
  - `L492` tracking row: same rename as L45
  - `L492` row status remains `pending` until this change's archive; earlier rows (`v2-composicion` etc.) must not be altered (preserve complete markers)
  - `L147-148` `v2-no-regresion/U-01` mentions `npm test` + `npm run typecheck` — stale TS suite; this is historical Deltas text for that spec, not part of D09's flow spec, so it stays verbatim per series convention (only flow-related stale is fixed; other specs' historical text remains). Lint: proposal must explicitly bound stale correction to flow-related lines only.
- **`AGENTS.md`** — single edit:
  - `L7` "including gentle-ai/openspec/SDD artifacts" → "including openspec/SDD artifacts" (remove provider prefix; preserve meaning). No other gentle-ai hit exists. `L23` stale `go.mod does not exist yet` can be corrected opportunistically as part of cleanup (si el proposal lo incluye) or deferred — not required by D09 criteria but is visible drift.
- **`openspec/` — explicitly EXCLUDED.** All 31 archive hits (`gentle-ai.verify-result/v1`, historical mentions of `v2-flujo-gentle-ai`) remain. No `openspec/specs/` file needs editing (0 hits there). The orphan check `grep -r v2-flujo-gentle-ai openspec/` confirms only archive holds the old id — proposal must document that archive preserves history and Deltas' rename does not retroactively alter closed archive reports.
- **`docs/v2/haro-constitucion.md` + `docs/v2/haro-especificacion-tecnica.md`** — 0 hits, no edit (verified). Proposal must assert this.
- **`scripts/` — new artifact for U-01:**
  - Path options: `scripts/verify-traceability.sh` (bash auditor) or `scripts/verify-traceability.go` / `internal/cmd/verify-traceability_test.go`. Recommended: bash script + Go test harness `internal/traceability/traceability_test.go` that invokes it via `exec.Command` so `go test ./...` covers it. Script logic: parse `deltas-acceptance.md` with `grep -n "^# Spec: v2-"` and `grep -n "^### F-\|^### U-"` to build ID set; join with spec `TRACE` tables and/or `go test -list` / `grep -rn "Test.*v2-flujo\|TestTrace"` to confirm each D09 criterion has ≥1 test; exit 1 listing orphan IDs. Global audit of all 92 IDs can be partial: D09 change must prove its 4 criteria have tests; full 92-ID green can be documented as table with deferred specs noted.
- **`internal/` — tests for U-01:**
  - If approach (a): add `internal/traceability/traceability_test.go` or `internal/cmd/traceability_test.go` with `TestCriterionTraceabilityAudit` that runs `bash scripts/verify-traceability.sh` and asserts exit 0; plus local tests for D09 itself (e.g., `TestFlujoSDD_F01_ChangePerSpec`, `TestFlujoSDD_F02_VerifyLayers`, `TestFlujoSDD_F03_NoInitiatives`, `TestFlujoSDD_U01_MappingExists`) as named tests matching the proposed spec's TRACE table. These give U-01 its required "at least one named test per criterion" in-process.
  - If approach (b): traceability tests are doc/table only (verify-report lists shell `grep` evidence, not a Go test), so `scripts/` gate may be absent.
- **`.atl/skill-registry.md`** — contains auto-generated `gentle-ai` prefix; not in scope, ignored. Proposal must note it is tooling-owned.
- **`.github/workflows/ci.yml`** — no gentle-ai mention, no edit needed; it is already the SDD-equivalent delivery gate for F-03. Proposal should document it as the gate evidence rather than edit it.
- **Out-of-scope guardrails (must NOT be touched):** `openspec/` files, `docs/v2/` constitution/spec, product code (`internal/workflow`, `internal/store`, `internal/adapter`, etc.), broker/IPC/PTY, new dependencies, `.haro/`, coverage artifacts.

## Approaches

### 1) Full rename + stale cleanup + executable traceability audit — Go-real audit script + spec table + CI gate (recommended)

- **Description:** Execute the full user-mandated rename and the logical stale correction, and close U-01 with a real executable audit: (a) edit `deltas-acceptance.md` on exactly the lines listed above (heading + §Usage + spec heading + F-02/F-03 + Conventions + tracking table, plus `node --test`→`go test ./...` on the flow-related lines only), (b) edit `AGENTS.md:7` to remove the provider prefix, (c) add `scripts/verify-traceability.sh` (bash, <100 lines) plus a Go test harness `internal/traceability/traceability_test.go` (or `internal/cmd/traceability_test.go`) with 4 named tests for D09's criteria that both assert mapping existence and invoke the script; script walks `deltas-acceptance.md` IDs, joins with `openspec/specs/*/spec.md` TRACE tables / test name greps, and fails on orphan IDs; wire the gate into `ci.yml` or rely on `go test ./...` covering it; document full 92-ID mapping status in the spec's traceability section (deferred specs flagged), and wire D09's local 4-criteria table to the 4 named tests.
- **Pros:** Directly closes all 4 criteria with verifiable evidence; satisfies U-01's exact verification clause "audit script that walks the criterion IDs and confirms their corresponding test exists and passes" with a real passing gate rather than prose; cleans the stale `node --test`/`TypeScript` drift that would otherwise misstate the runner to any new contributor; single provider-name-free vocabulary thereafter; follows series size budget (audit adds ~150-250 lines, within `review_budget_lines: 20000` and `single-pr` flow; precedent `v2-adapter` shipped 3283 lines under `size:exception`).
- **Cons:** Larger surface than pure doc rename (needs script + Go test + spec table); audit of all 92 IDs will surface pending/deferred gaps (`v2-broker`/`v2-ipc` deferred, `v2-no-regresion`/`v2-adapter` rows still pending) that must be documented as expected partial rather than failures — script must allow "deferred" allowlist or two-tier verification (D09's 4 strict, others informational). Slightly more review than doc-only.
- **Effort:** Medium. Rename edits ~12 lines across 2 files; §Usage rewrite ~5 lines; audit script ~60-80 lines; Go harness + 4 local tests ~80-120 lines; spec/design/tasks/verify artifacts per SDD.

### 2) Rename + stale cleanup + documentary traceability — table-only proof, no executable audit script

- **Description:** Perform the same rename and `node --test`→`go test ./...` stale correction (edits identical to Approach 1), and satisfy U-01 with a prose table in the spec + `verify-report.md` evidence (e.g., `verify-report` lists the 4 D09 criteria with shell `grep` command output and "one named test per criterion" rows, but no committed `scripts/verify-traceability.sh` and no Go test that fails closed). Distribution of `v2-flujo-sdd` otherwise identical; global 92-ID audit is presented as a markdown table, not a runnable gate.
- **Pros:** Smallest diff that still renames and fixes stale; trivially passes D09's local F-01/F-02/F-03 (process) criteria; avoids building/maintaining an audit script; avoids triaging the 92-ID deferred/pending allowlist logic.
- **Cons:** Leaves U-01's verification clause literally unsatisfied — the clause explicitly calls out an "audit script"; a table plus `grep` transcript is reviewer-judged, not machine-enforced, and would likely be flagged as `pass_with_warnings` or `blocked` in a strict `sdd-verify` envelope; weaker regression value (future criteria additions would not be caught by CI); constitution traceability principle (I.1 auditable traceability) prefers machine-auditable proof.
- **Effort:** Low. Rename + stale edits ~12 lines; spec table + verify-report prose only.

### 3) Rename only — provider name removed, stale and audit deferred

- **Description:** Do exactly the user-mandated minimal rename: replace `gentle-ai` occurrences in `deltas-acceptance.md` (9 hits) and `AGENTS.md:7` (1 hit) with neutral wording (`SDD` / "development flow"), but leave `node --test`/`npm test`/`TypeScript`/`tsc --noEmit` (L5, L25, L54, L147-148) unchanged and leave U-01 without new artifacts (no script, no table). D09's change would document the rename and defer traceability/stale to future changes.
- **Pros:** Minimal risk to `deltas-acceptance.md` (only mechanical token replacement, no intent change); trivial review; archive linkage preserved with smallest edit distance.
- **Cons:** Fails to close U-01 entirely (no named test per criterion, no audit script → U-01 stays pending, so D09 cannot be marked complete); leaves the most visible drift (`node --test` as the stated runner for a Go repo) uncorrected, so a new contributor reading `deltas-acceptance.md:54` would run the wrong command and misinterpret `[E2E]`/`[UNIT]` layers; F-02's runner description would remain false after the rename, preserving the inconsistency that v2-flow is supposed to eliminate. Requires a follow-up change for U-01 + stale, fragmenting the last spec across two changes.
- **Effort:** Very Low (~10 line renames). Negative completeness.

### 4) Full historical rewrite — rename including archive and docs/v2 (documented only to reject)

- **Description:** Like Approach 1 but also rewrites `openspec/changes/archive/*` ingestion (`gentle-ai.verify-result/v1` → `sdd.verify-result/v1`), replaces `v2-flujo-gentle-ai` mentions in archived exploration/spec/design files, and rewrites `docs/v2/` preambles to remove historical provider notes.
- **Pros:** Full uniformity of vocabulary across every file in the repo.
- **Cons:** **Explicitly forbidden** by user decision ("TODO lo de `openspec/` queda EXCLUIDO — archivos históricos de archive se conservan tal cual") and by SDD archive immutability convention (archive is a terminal record; rewriting it would invalidate `evidence_revision` hashes in `verify-report.md` / `archive-report.md` and break traceability). Would also violate the `deltas-acceptance.md` / `AGENTS.md` scope limit. Negative value.
- **Effort:** High, destructive. **Rejected — included only to make explicit why it is not pursued.**

## Recommendation

**Adopt Approach 1 (full rename + stale cleanup + executable traceability audit).**

**Justification:**

- **U-01 demands a machine gate, not prose.** The contract's U-01 verification clause is the only one among D09's 4 that explicitly says "audit script that walks the criterion IDs and confirms their corresponding test exists and passes." The series precedent for verification gates (`verify-adapter-boundary.sh`, `verify-store-boundary.sh`, `verify-distribution.sh`) is a bash script + Go test that fails closed; the same pattern must close U-01, otherwise `sdd-verify` would remain `pass_with_warnings` for the final spec — poor closure for the 92-criteria series.
- **Stale `node --test` is not cosmetic.** `deltas-acceptance.md:54` is the normative mapping "what counts as E2E vs UNIT verification" and is referenced by every future contributor's `sdd-verify` run. Leaving it as `node --test` for a `go test ./...` repo silently breaks F-02's operational meaning. The user decision scopes cleanup to `deltas-acceptance.md` + `AGENTS.md` only, which is exactly where stale lives; `docs/v2/` already has 0 hits and `openspec/` is correctly excluded, so the stale correction is fully within the allowed scope.
- **Rename is non-optional and isolated.** The 9 hits in `deltas-acceptance.md` + 1 in `AGENTS.md` are the only provider-named surfaces in the editable scope; archive hits stay untouched per user, so traceability break risk is limited to the tracking table's id rename (`v2-flujo-gentle-ai` → `v2-flujo-sdd`). Since archive preserves the old id as history, the proposal can document the alias (`previously v2-flujo-gentle-ai (D09), now v2-flujo-sdd`) without rewriting history — the linkage remains auditable.
- **Delivery hybrid is already compliant with F-03.** No new work is needed beyond documenting the existing branch-protection + CI gates as the flow's gates and asserting `.docs/initiatives/` absence; the audit script adds a machine check for that absence too, making F-03 closure cheap.
- **Size and process fit.** Estimated diff for Approach 1 (~150-250 lines for audit + ~12 lines for rename/stale + normal SDD artifacts) is well within `review_budget_lines: 20000`, `single-pr` delivery and the repo's linear-history + required-reviews gates. It mirrors the complete vertical slice style used by `v2-composicion`/`v2-reporte`/`v2-store`/`v2-distribucion` rather than fragmenting D09 across two changes.

**Scope for `sdd-propose`:** Proposal must enumerate the exact `deltas-acceptance.md` line targets listed above (L7, L19, L45, L49, L51, L54, L456, L463, L467, L492, plus L5/L25 if stale cleanup includes them — bounded decision), the single `AGENTS.md:7` hit, the `scripts/verify-traceability.sh` + Go test harness design (including 92-ID vs 4-ID two-tier verification and deferred-specs allowlist), and the out-of-scope guardrails (no `openspec/` archive rewrite, no `docs/v2/` edit, no product-code change, no new dependencies, no PTY/broker).

## Risks

- **Tracking-id rename breaks cross-reference links (MEDIUM, mitigable):** Changing `v2-flujo-gentle-ai` → `v2-flujo-sdd` in the tracking table and the spec heading means any historical commit message, archive report, or Engram observation that mentions `v2-flujo-gentle-ai` will no longer match the live `deltas-acceptance.md` id by string search. Mitigation: proposal records `v2-flujo-sdd (previously v2-flujo-gentle-ai, D09)` as an alias in the spec's front matter and in `archive-report.md` upon close; archive files remain unedited so history is preserved; Engram memory search for `v2-flujo-gentle-ai` will still find historical context — document the alias in the spec's Traceability header.
- **92-ID audit surfaces pre-existing pending/deferred rows as "failures" (MEDIUM, mitigable):** A naive `audit script walks the criterion IDs` that requires every one of the 92 to have a passing named test today would fail because `v2-broker`/`v2-ipc` are deferred (deferred by CLI-direct architecture per every archive report since `2026-08-28`) and `v2-no-regresion`/`v2-adapter`/`v2-path-claims` still show `pending` in `deltas-acceptance.md` despite being archived (tracking update began only at `v2-composicion`). Mitigation: script implements two tiers — (a) strict gate: D09's own 4 criteria must each have ≥1 named test and all pass, (b) full-catalog report: 92-ID mapping with status flags `pass | deferred (CLI-direct) | archived-pending (row not updated) | missing`; CI gate enforces only tier (a), tier (b) is informational and documents the honest closure state.
- **Over-correcting stale beyond the flow surface (LOW-MEDIUM, guardrail):** Stale spans both flow-specific lines (L5, L25, L54) and per-spec historical lines (e.g., `v2-no-regresion/U-01` L147-148 describing `npm test` + `npm run typecheck` for the 550-case suite). The user scope says "limpieza SOLO `deltas-acceptance.md` y `AGENTS.md`" but does not bound which paragraphs inside `deltas-acceptance.md`. Editing per-spec historical text beyond the flow section would rewrite v1→v2 heritage that intentionally remains as oracle. Mitigation: proposal explicitly limits stale correction to flow-surface lines (header Purpose/Conventions/§Usage + D09's F-02 runner note); all other specs' `npm test` literals remain verbatim; the gate/grep for stale verifies only `§Usage` and header, not the entire file.
- **Neutral wording inconsistency ("Sustainability" vs "SDD flow" vs "development flow") (LOW, mitigable):** Candidates for neutral replacement are `SDD`, `development flow`, `flujo SDD`. Mixing them across the 9 hits would create an inconsistent contract. Mitigation: proposal picks one canonical neutral term (`SDD flow` / `flujo SDD`) and applies it uniformly: heading `## Usage with gentle-ai` → `## Usage`, inline references → "SDD flow" (EN) / "flujo SDD" (ES context inside E2E mapping), and documents the equivalence "SDD flow is the neutral name for the previously documented gentle-ai flow" in the spec's glossary.
- **Audit script couples to `deltas-acceptance.md` markdown shape (LOW):** A regex-based parser that extracts IDs via `^### F-0` may break if future criteria use different heading depth or inline tables. Mitigation: script is deliberately tolerant (matches `^###\s+(F|U)-[0-9]+` and `^#\s+Spec:\s+v2-` anywhere, plus table-row `\| `v2-` patterns) and is tested against the current file's 92 IDs plus a fixture with the v1 edge cases; parser is kept local to this change, not shared infra.
- **Delivery F-03 interpreted as "no PR required" (LOW):** Recent archive commits pushed directly to `main` could be read as bypassing F-03's review-receipt gate. Mitigation: proposal clarifies that F-03's gate is branch protection (`required_pull_request_reviews` + `required_linear_history` + `required_conversation_resolution`) and that direct pushes are permitted only for `docs(sdd): archive …` terminal records after `sdd-verify` passed — feature code always goes via PR with review (evidenced by `feat/v2-*` PRs). Audit script checks `gh api branches/main/protection` or `.github/workflows/ci.yml` required contexts rather than commit graph.
- **No product-code temptation (LOW):** D09 is a process spec; there is no feature code to implement. Contributors may be tempted to "improve" `internal/*` to satisfy traceability (e.g., add dummy named tests that do not prove anything). Mitigation: proposal scopes `tasks` to docs + auditor + spec traceability only; `openspec/changes/v2-flujo-sdd/tasks.md` lists no product-code tasks, only `deltas-acceptance.md` / `AGENTS.md` edits, `scripts/verify-traceability.sh` + Go harness, spec table, and verify/archive docs.

## Boundaries / Non-Goals (confirm with sources)

- **No `openspec/` rewrite** — `openspec/changes/archive/` retains `gentle-ai.verify-result/v1` envelopes, `v2-flujo-gentle-ai` mentions and `gentle-ai sdd-verify-validate` strings verbatim (user decision + SDD archive immutability). `openspec/specs/` already neutral (0 hits) and must not gain a `v2-flujo-gentle-ai` legacy alias file. Verified via `grep -r`.
- **No `docs/v2/` edit** — `haro-constitucion.md` / `haro-especificacion-tecnica.md` contain 0 `gentle-ai` hits; they are not touched. Confirmed via `grep -n`.
- **No product code / no new dependencies** — Pure doc + auditor change. No `internal/workflow`, `internal/store`, `internal/adapter`, `internal/ipc`, `internal/cmd` behavioral edit. No `go.mod` dependency added. PTY/broker/delivery artifact scope remains deferred per `AGENTS.md` Out-of-scope and `deltas-acceptance.md:497-501`.
- **No initiatives pipeline recreation** — `.docs/initiatives/` must remain absent; auditor will assert absence. No `tools/scripts/initiative/` path created.
- **No archive-hash invalidation** — Archive `evidence_revision` sha256 hashes in `verify-report.md`/`archive-report.md` are historical snapshots; rewriting archive would invalidate them. This change's own new `evidence_revision` will be its own HEAD commit after `sdd-verify` on the new D09 change — no retroactive mutation.
- **No cross-cutting Deltas row mutation beyond D09** — Tracking rows for `v2-reconciliacion`…`v2-distribucion` stay `complete`/`pending` as last updated `2026-09-07`; only the final row `v2-flujo-gentle-ai → v2-flujo-sdd` changes id/name (status remains `pending` until this change's archive, then → `complete`).
- **No `.atl/skill-registry.md` ownership** — Auto-generated `gentle-ai` header there is tooling-owned, explicitly out of scope.
- **No English/Spanish mismatch drift** — Deltas is written in English per `AGENTS.md` Language policy; neutral rename retains English in `deltas-acceptance.md` and `AGENTS.md`, while the proposal may note Spanish equivalences ("flujo SDD") only inside explanatory paragraphs, not as contract terms.

## Open Questions for Proposal

1. **Exact neutral term** — Should the contract use `SDD flow` (neutral, tool-agnostic), `development flow` (generic), or `flujo SDD` as the canonical replacement for `gentle-ai flow` across the 9 hits, and should `§Usage with gentle-ai` become `§Usage` or `§SDD development flow`?
2. **Stale scope boundary** — Does `sdd-propose` include correcting `deltas-acceptance.md:5` ("TypeScript is kept — no rewrite") and `:25` (full TypeScript stack paragraph) as part of the flow cleanup, or limit correction strictly to `:54`/`L463` runner lines, leaving the header stack note as historical context?
3. **`AGENTS.md:23` secondary stale** — Should the proposal opportunistically fix "`go.mod does not exist yet — it will be created in the spike phase`" (false since `v2-spike-go` 2026-08-28) while editing `AGENTS.md:7`, or defer it to keep the `AGENTS.md` edit minimal and strictly `gentle-ai`-related?
4. **92-ID audit tiering** — Should the committed audit script enforce strict pass on all 92 IDs today (would fail on deferred `v2-broker`/`v2-ipc`) or enforce only D09's 4 with informational report for the remaining 88, and how should deferred criteria be tagged (`deferred:cli-direct` vs `pending:row-not-updated`)?
5. **Harness for U-01's named tests** — Should D09's 4 named tests live as `internal/traceability/traceability_test.go` (new package), `internal/cmd/flow_traceability_test.go` (co-located with existing F-02/F-03 boundary tests), or as `scripts/verify-traceability.sh` only with no Go test (doc-only U-01 proof)?
6. **CI wiring** — Should the traceability auditor be added as a new `ci.yml` job/step (like `distribution gate`) that runs `bash scripts/verify-traceability.sh`, or is passing `go test ./... -race` (which runs the Go harness that in turn runs the script) sufficient as the gate?
7. **Tracking row timing** — Should the `deltas-acceptance.md:492` tracking row `pending → complete` flip happen at `apply` (after `verify` passes) or only at `archive` (as done by `v2-composicion` onward), and should the alias `previously v2-flujo-gentle-ai` be kept as an inline note in the renamed row?
8. **Envelope schema rename** — `gentle-ai.verify-result/v1` appears inside archived `verify-report.md` envelopes but also as the `openspec/config.yaml` verify envelope name. Should this change document that the envelope schema remains `gentle-ai.verify-result/v1` for historical archive compatibility, with a future additive `sdd.verify-result/v1` alias, or leave it untouched?

## Ready for Proposal

**Yes.** Current state is fully verified (exact `gentle-ai` lines, stale runner, real Go gates, no auditor, prior traceability pattern, archive exclusion), gaps are bounded to a 12-line doc rename/stale correction plus a scoped auditor with two-tier 92/4 verification, limits are crisp (no `openspec/`/`docs/v2`/product-code/new-deps/PTY), and the central trade-off (executable audit vs table-only) is resolved in favor of a real gate per U-01's literal clause and the gate-script precedent. `sdd-propose` can formalize the rename targets, stale surface, auditor contract, and guardrails without re-research.

